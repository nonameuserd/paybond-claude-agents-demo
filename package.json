{
  "name": "paybond-claude-agents-demo",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "smoke": "paybond agent sandbox smoke --preset travel --result-body '{\"status\":\"completed\",\"cost_cents\":18700}' --format json"
  },
  "dependencies": {
    "@paybond/kit": "^0.10.0",
    "@anthropic-ai/claude-agent-sdk": "^0.2.100",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22"
  }
}
